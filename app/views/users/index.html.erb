<div class="title">
  The User_Derpers List of Magical Users
</div>

<div class="order-pod">
  <div class="information-bar">User Details</div>
  <div class="headed-box">
    <div class="content">We currently have <%= @users.uk.count %> UK Users registered</div>
    <div class="content">We currently have <%= @users.se.count %> SE Users registered</div>
    <div class="content">We currently have <%= @users.no.count %> NO Users registered</div>
  </div>
</div>

<div class="order-pod">
  <div class="information-bar">Users on On the Beach</div>
  <div class="pagination"><%= will_paginate @users %></div>
  <table>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Email</th>
      <th>Domain</th>
      <th colspan="3">Actions</th>
    </tr>

    <% @users.each do |user| %>
      <tr>
        <td><%= user.first_name.capitalize %></td>
        <td><%= user.surname %></td>
        <td><%= user.email.capitalize %></td>
        <td><%= render user.flag %></td>
        <td><%= link_to 'Show', user_path(user) %></td>
        <td><%= link_to 'Edit', edit_user_path(user) %></td>
        <td><%= link_to 'Destroy', user_path(user),
                method: :delete,
                data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </table>
</div>

RAW DATA DUMPS TO BE DELETED LATER<br />
feed
<%= line_chart User.group_by_day(:created_at).count %> <br />
<%= line_chart LoginHistory.group_by_day(:logged_in).count %><br />
<%= pie_chart User.group(:domain).count %><br />
<%= column_chart Search.group_by_day_of_week(:departure_date, format: "%a").count,
                 id: "column-chart-idhere-1", width: "800px", height: "500px" %><br />
create
<%= pie_chart({"Football" => 10, "Basketball" => 5}) %><br />
THIS NEEDS THE GOOGLE JS API
<%= timeline [
                 ["Washington", "1789-04-29", "1797-03-03"],
                 ["Adams", "1797-03-03", "1801-03-03"],
                 ["Jefferson", "1801-03-03", "1809-03-03"]
             ] %><br />
new route
<%= line_chart login_histories_charts_path %>
